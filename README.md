# the-clusterizer
Given a jira instance url, a pat and a query filter, this project will create clusters of ticket types using AI so that you can know which type of tickets are impacting you more. Frontend vanilla typescript, backend whatever ai chooses and pgsql as both db and vector db
